============
Installation
============

At the command line::

    $ pip install listenclosely

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv listenclosely
    $ pip install listenclosely
    

